/** Fallback for an entry that doesn't set its own — the hero's periwinkle, so an unstyled item
 *  still reads as this site's material rather than a gray default. */
export const DEFAULT_ACCENT = "#8ea8f5";
export const DEFAULT_ACCENT_SOFT = "#bccbfa";
